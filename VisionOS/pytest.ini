[pytest]
testpaths = tests
addopts = -q
markers =
    slow: cần detector thật (YOLOv8/ultralytics) — bỏ qua nếu chưa cài
filterwarnings =
    ignore:.*ByteTrack.*:Warning
    ignore::DeprecationWarning
