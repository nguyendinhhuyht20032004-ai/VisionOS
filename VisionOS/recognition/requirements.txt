# --- Bắt buộc để chạy phần đếm + toàn bộ test (không cần GPU) ---
numpy>=1.24
pytest>=7.0

# --- Tuỳ chọn: chỉ cần khi chạy MÔ HÌNH THẬT (import lazy) ---
# YOLO chế độ STANDARD (người/xe) — CHỌN 1 trong 2 backend:
#   (khuyến nghị) ultralytics YOLOv8 — cài ổn định trên Kaggle/Colab:
# ultralytics
#   hoặc YOLO-NAS (super-gradients) — hay xung đột phiên bản, cần kiên nhẫn:
# super-gradients==3.7.1
# torch>=2.0

# LocateAnything-3B (chế độ SMART: open-vocabulary) — dùng lại la_counting.detector
# transformers==4.57.1
# accelerate

# Đọc/ghi video khi chạy thật
# opencv-python-headless
