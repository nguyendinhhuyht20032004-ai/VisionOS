# --- Bắt buộc để chạy phần đếm + toàn bộ test (không cần GPU) ---
numpy>=1.24
pytest>=7.0

# --- Tuỳ chọn: chỉ cần khi chạy MÔ HÌNH THẬT trên GPU (import lazy) ---
# YOLO-NAS (chế độ STANDARD: người/xe)
# super-gradients==3.7.1
# torch>=2.0

# LocateAnything-3B (chế độ SMART: open-vocabulary) — dùng lại la_counting.detector
# transformers==4.57.1
# accelerate

# Đọc/ghi video khi chạy thật
# opencv-python-headless
