# Dependencies cho chay NATIVE tren macOS (Apple Silicon / CoreML).
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-native.txt
#
# Sau do export model:
#   python export_coreml.py --weights yolov8m.pt
#
# Chay service:
#   ./run_native.sh
# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
# --- AI: YOLO + tracking ---
ultralytics>=8.2
supervision>=0.21
opencv-python-headless>=4.9
numpy>=1.24
# PyTorch: can cho ultralytics load model .pt va export.
# Tren Apple Silicon, pip install torch se lay ban CPU+MPS (Metal) tu dong.
torch>=2.2
torchvision>=0.17
# --- CoreML export + inference ---
coremltools>=7.0
# --- Data ---
qdrant-client>=1.9
redis>=5.0.0
