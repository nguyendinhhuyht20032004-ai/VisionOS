# Dependencies cho AI SERVICE (recognition/service) -- chay native.
#   python -m venv .venv && .venv\Scripts\activate  (Windows)
#   pip install -r requirements-native.txt
#
# Sau do export model:
#   python export_openvino.py --weights yolov8m.pt
#
# Chay service:
#   python run_service.py
# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
# --- AI: YOLO + tracking ---
ultralytics>=8.2
supervision>=0.21
opencv-python-headless>=4.9
numpy>=1.24
# PyTorch: can cho ultralytics load model .pt va export.
torch>=2.2
torchvision>=0.17
# --- OpenVINO inference (thay CoreML) ---
openvino>=2024.0
# --- gRPC (thay Redis) ---
grpcio>=1.60
grpcio-tools>=1.60
protobuf>=4.25
# --- Data (vector DB -- giu lai, chua ap dung) ---
qdrant-client>=1.9
