# Phu thuoc cho AI SERVICE dem theo camera (recognition/service) -- dung cho Docker.
#   pip install -r requirements-service.txt
# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9          # nhan upload anh/frame
# --- Thi giac may: detect (YOLO) + track/dem (supervision) ---
ultralytics>=8.2                 # YOLOv8 / YOLO11
supervision>=0.21                # ByteTrack + LineZone + PolygonZone + annotator
opencv-python-headless>=4.9
numpy>=1.24
# torch + TORCHVISION: PHAI cai CUNG LUC, KHOP phien ban.
torch>=2.2
torchvision>=0.17
# --- OpenVINO inference ---
openvino>=2024.0
# --- gRPC (thay Redis) ---
grpcio>=1.60
grpcio-tools>=1.60
protobuf>=4.25
# --- Vector database (giu lai, chua ap dung) ---
qdrant-client>=1.9
