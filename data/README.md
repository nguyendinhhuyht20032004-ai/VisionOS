# Thư mục video cho service (mount vào container tại /data)

Đặt file video vào ĐÂY rồi trên web nhập nguồn = `/data/<tên>.mp4`.
Container KHÔNG thấy ổ đĩa máy Mac/Windows, nên phải để video trong thư mục này.

Ví dụ: copy `traffic.mp4` vào `data/` → nhập nguồn `/data/traffic.mp4` → "Lấy frame".
